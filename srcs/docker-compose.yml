services:
  nginx:
    image: srcs-nginx
    # build: ./requirements/nginx
    ports:
      - "8080:80"
  wordpress:
    # image: srcs-wordpress
    build: ./requirements/wordpress
    ports:
      - "9000"
  mariadb:
    # image: srcs-mariadb
    build: ./requirements/mariadb
    ports:
      - "3306"
