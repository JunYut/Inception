worker_processes 1;

events {
	worker_connections 1024;
}

http {
	server {
		listen 80;
		server_name localhost;

		root /var/www/wordpress;
		index index.php index.html;

		location / {
			root /var/www/wordpress;
		}
	}
}
